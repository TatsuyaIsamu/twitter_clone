<div class="container">
  <% @post.errors.full_messages.each do |msg| %>
    <%= msg%>
  <% end %>
  <%= form_with(model:@post, local: true) do |form| %>
    <%= form.label :content %>
    <%= form.text_field :content %>
    <%= form.submit "つぶやく" %>
  <% end%>
</div>