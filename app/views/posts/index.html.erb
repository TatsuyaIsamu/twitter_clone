<div class="container" >
  <h1>つぶやき一覧</h1>  <p class="new_tweet">つぶやく</p>
    <table class="index_table" >
      <% @post.each do |post| %>
        <tr><td><%= post.content%></td><td>一覧表示</td><td>編集</td><td>削除</td></tr>
      <% end%>
    </table>
</div>